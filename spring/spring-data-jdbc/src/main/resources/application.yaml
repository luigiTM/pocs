spring:
  h2:
    console:
      path: /h2-console
      enabled: true
  datasource:
    url: jdbc:h2:mem:datajdbc_poc
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always